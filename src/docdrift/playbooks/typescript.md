## Language-specific guidance: TypeScript / JavaScript

- List exported functions, components, and types under "Public interface".
- For React components, document the props interface rather than the internal
  rendering logic in detail.
- Distinguish between `interface`/`type` definitions (data structures) and
  actual logic.
