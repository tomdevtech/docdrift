# docdrift
Docdrift keeps your project documentation in sync with your code by automatically detecting drift and updating docs via a local LLM or GitHub Actions, whether that's private technical notes in Obsidian or product docs in your repo.
