# Detailed usage

> TODO: Once `init` and `run` are implemented, add step-by-step instructions,
> including screenshots and sample output.

## Planned topics

- Prerequisites (Ollama/LM Studio running locally with a model downloaded)
- `docdrift init` in detail
- Creating custom playbooks for a project
- Working with the `<!-- AUTO-DOC-END -->` marker for manual notes
- Troubleshooting (model server unavailable, hook not running, and so on)
